# Exercise 0.6: New note in Single page app diagram
```mermaid
sequenceDiagram
    participant browser
    participant server

    Note right of browser: Sends note 
    browser ->> server: Sends note
    activate server
    server -->> browser: 201 HTTP response
    deactivate server
    Note right of browser: Thanks to the Js script previously fetched, looking for the on form.onsubmit handler and pushes the new note in a list to avoid  a GET requests from the server
    activate server
    browser ->> server: POST request of the notes list to the server
    deactivate server
